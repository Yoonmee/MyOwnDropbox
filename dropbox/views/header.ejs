<!-- Navigation -->

<% if(!session.user_info) { %>
<!-- 로그인 안하면 기본 색-->
<% } else if (session.user_info.user_theme == 1) { %>
<!-- 벚꽃색 -->
  <style>
  .navbar{
    background-color: #FFB6C1
  }
  </style>
<% } else if (session.user_info.user_theme == 2) { %>
<!-- 하늘색 -->
  <style>
  .navbar{
    background-color: #00EBFF
  }
  </style>
<% } else if (session.user_info.user_theme == 3) { %>
  <!-- 연두색 -->
    <style>
    .navbar{
      background-color: #80E12A
    }
    </style>
<% } else { %>
  <!-- 기본색 -->
<% } %>

<div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="index">My Own Dropbox</a>
    </div>
    <!-- /.navbar-header -->

    <ul class="nav navbar-top-links navbar-right">


        <!-- /.dropdown -->
        <li class="dropdown">
            <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <i class="fa fa-user fa-fw"></i> <i class="fa fa-caret-down"></i>
            </a>
            <ul class="dropdown-menu dropdown-user">
              <% if(!session.user_info) { %>
                <li><a href="login"><i class="fa fa-sign-out fa-fw"></i> Login</a>
                </li>
                  <% } else { %>
                <li><a href="mypage"><i class="fa fa-user fa-fw"></i><%= session.user_info.user_name %> 님</a>
                </li>
                <li><a href="#"><i class="fa fa-gear fa-fw"></i> Settings</a>
                </li>
                <li class="divider"></li>
                <li><a href="logout"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
                </li>
                <% } %>
            </ul>
            <!-- /.dropdown-user -->
        </li>
        <!-- /.dropdown -->
    </ul>
    <!-- /.navbar-top-links -->
